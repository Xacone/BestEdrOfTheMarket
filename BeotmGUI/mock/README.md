# Mock BEOTM server

This python project aims to simulate the production of events from the BEOTM server.

Dependencies are managed with [uv](https://docs.astral.sh/uv/)

- **Start the server** with `uv run --frozen server.py` (it will create the project venv and install the dependencies the first time)